


class DffExporter:
	
	def __init__(self, deformation, configuration, outputfile):
		self.deformation = deformation
		self.configuration = configuration
		self.exportparameters = exportparameters
		self.outputfile = outputfile
		

	